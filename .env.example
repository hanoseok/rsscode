# Database path (Docker volume 사용시 /data/rsscode.db)
DATABASE_URL=./data/rsscode.db

# Server port
PORT=3000

# RSS 체크 주기 (cron 표현식, 기본: 10분마다)
CRON_SCHEDULE=*/10 * * * *

# Discord Client ID/Secret은 웹 UI Settings에서 설정
